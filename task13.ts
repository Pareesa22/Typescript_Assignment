let favoriteTransportation: string[] = ["Honda Moorcycle", "corolla","toyota car"];

// Iterate through the array and print statements about each item
favoriteTransportation.forEach(transport => {
    console.log(`I would like to own a ${transport}.`);
});